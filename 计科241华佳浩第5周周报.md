# 计科241 华佳浩 — 第5周周报

**主题：Web 安全基础——CSP 策略、XSS/CSRF 防御、SameSite Cookie 与信任边界**

## 概要

本周围绕 Web 安全最常见的两类攻击（XSS/CSRF）展开，重点学习了浏览器侧的 **CSP（内容安全策略）** 与 **SameSite Cookie**，并用 **信任边界（Trust Boundary）** 的思维梳理“哪些数据不可信、哪些环节必须校验”。目标是形成一套可落地的安全清单，能直接用于项目自查与加固。

---

## 1. Web 安全攻防视角

- **XSS（跨站脚本）**：把“不可信数据”变成“可执行脚本”，在用户浏览器里运行。
- **CSRF（跨站请求伪造）**：利用浏览器自动携带 Cookie 的特性，借用用户登录态替用户发起操作。
- **核心原则**：**不要相信任何来自信任边界之外的输入**；前端校验只为体验，后端校验才算安全。

---

## 2. CSP 策略（Content Security Policy）

CSP 是浏览器的“资源加载/脚本执行白名单”，通过限制脚本来源、禁止内联脚本等方式显著降低 XSS 成功率与破坏面。

### 2.1 常用指令速查

| 指令                       | 作用               | 推荐值              | 备注                                     |
| -------------------------- | ------------------ | ------------------------------- | ---------------------------------------- |
| `default-src`              | 默认资源加载策略   | `'self'`                        | 先从“只信自己站点”开始                   |
| `script-src`               | 脚本来源与执行规则 | `'self' 'nonce-...'`            | 推荐使用 nonce；尽量避免 `unsafe-inline` |
| `style-src`                | CSS 来源           | `'self'`（必要时加 nonce/hash） | 逐步收敛第三方来源                       |
| `img-src`                  | 图片来源           | `'self' data:`                  | 视业务是否需要 `data:`                   |
| `object-src`               | 插件内容           | `'none'`                        | 基本直接禁用                             |
| `base-uri`                 | `<base>` 规则      | `'self'`                        | 防止 base 标签被利用                     |
| `frame-ancestors`          | 允许谁嵌入 iframe  | `'none'` 或 `'self'`            | 防点击劫持（优于 X-Frame-Options）       |
| `report-to` / `report-uri` | 上报违规           | 配置上报地址                    | 用于灰度观察与收敛                       |

### 2.2 落地建议

1. **先上 Report-Only**：不拦截，只收集违规日志，观察有哪些第三方资源/内联脚本。
2. **收敛白名单**：逐步把 `script-src` / `style-src` 的来源缩小到必要范围。
3. **引入 nonce/hash**：对必须存在的内联脚本/样式用 nonce/hash 放行。
4. **切换到 Enforce**：把策略从 Report-Only 转为真正生效（拦截违规资源）。

---

## 3. XSS 防御（Cross-Site Scripting）

### 3.1 常见类型与高危点

- **反射型**：URL 参数被回显到页面。
- **存储型**：不可信内容入库后展示给多个用户（危害更大）。
- **DOM 型**：前端用 `innerHTML`/字符串拼接 DOM 导致执行。

高危点：**把不可信数据拼进 HTML/属性/JS 字符串/URL**。

### 3.2 防御优先级

1. **输出编码/转义（核心）**：根据上下文做正确编码  
   - HTML 文本 → 转义 `< > & " '`
   - HTML 属性 → 属性转义 + 白名单
   - URL 参数 → URL encode
2. **避免危险 API**：尽量用 `textContent` / 安全模板渲染替代 `innerHTML`。
3. **富文本必须 sanitizer**：白名单过滤 + 严格限制标签/属性（例如只允许 `b/i/a` 等）。
4. **CSP 作为“减灾”**：阻止 inline 与不可信脚本源，降低命中率与影响面。
5. **Cookie 加固**：`HttpOnly` 防止 JS 读 Cookie（但不能阻止 XSS 本身）。

> 只做“输入过滤”并不可靠。

---

## 4. CSRF 防御（Cross-Site Request Forgery）

### 4.1 原理一句话

浏览器对目标站发请求时会**自动携带 Cookie**，攻击者诱导用户访问恶意页面，就可能在用户不知情时完成“借刀杀人”的操作。

### 4.2 典型防御组合

- **CSRF Token**：页面/请求携带 token，服务端校验。
- **SameSite Cookie**：跨站请求默认不带 Cookie，降低 CSRF 成功率。
- **校验 Origin/Referer**：对敏感写操作校验来源域名。
- **写操作不用 GET**：有副作用操作统一使用 POST/PUT/DELETE，并做权限校验与审计。

---

## 5. SameSite Cookie

SameSite 控制跨站请求是否携带 Cookie，是对 CSRF 的重要补强。

| SameSite 值 | 跨站请求携带 Cookie？    | 适用场景              | 风险/注意                   |
| ----------- | ------------------------ | --------------------- | --------------------------- |
| `Strict`    | 基本不带                 | 极安全的后台/管理站点 | 可能影响跳转登录体验        |
| `Lax`       | 多数跨站不带（常用默认） | 一般业务会话 Cookie   | 个别“顶级导航 GET”可能带    |
| `None`      | 会带（但必须 `Secure`）  | 第三方登录/跨站嵌入等 | CSRF 风险更高，需配合 token |


---

## 6. 信任边界与实现细节

信任边界：从这里开始数据/请求不再可信，跨过边界就必须做校验与最小权限控制。

### 6.1 典型边界

- **浏览器 → 后端接口**：用户可控输入（必须鉴权、校验、限流、审计）。
- **第三方回调 → 你的系统**：必须验签、校验时间戳/nonce 防重放。
- **服务 A → 服务 B**：内部也不天然可信（需要服务间鉴权、最小权限、mTLS/网关策略）。

### 6.2 实现细节

- 后端统一做：**鉴权（是谁）+ 授权（能做啥）+ 输入校验（数据合法）**
- 敏感接口加：**幂等/防重放、审计日志、速率限制**
- 不把前端当安全边界：前端校验用于体验，后端必须再校验一遍

